const EditUserDetails: React.FC = () => {
    return (
      <div>
        <h1>EditUserDetails</h1>
  
        <p>Add the bootstrap form </p>
        <p>on load connect to the rest api -- fetch the data</p>
        <p>prefill the form fields with the data</p>
        <p>make the form fields editable</p>
        <p>work on validations</p>
        <p>work on form submission</p>
        <p>after form submitted -- connect to rest api with PUT / PATCH</p>
        <p>wait for the response </p>
        <p>if success, show a message updated successfully for 3-4 seconds. </p>
        <p> then automatically redirect to the prev page (through history)</p>
      </div>
    );
  }
  
  export default EditUserDetails